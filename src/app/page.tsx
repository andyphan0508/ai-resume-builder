import Footer from "@/components/Footer";
import Hero from "@/components/Hero";
import Navbar from "@/components/NavBar";
import Image from "next/image";

const Home = () => {
  return (
    <>
      <Navbar />
      <Hero />
      <Footer />
      
    </>
  );
};

export default Home;
// This is a simple Next.js page that displays a centered message indicating that Tailwind CSS is working.
// It uses Tailwind CSS for styling and is structured to be responsive and visually appealing.
// The page is designed to be a starting point for further development in a Next.js application.
